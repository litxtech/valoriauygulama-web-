import { PartnerProfileContent } from '@/components/breakfastPartner/PartnerProfileContent';

/** Onay bekleyen partner — profil düzenleme (geri tuşu ile pending'e dönüş). */
export default function PartnerEditProfileScreen() {
  return <PartnerProfileContent showBack />;
}
