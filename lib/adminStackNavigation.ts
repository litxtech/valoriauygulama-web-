/**
 * Admin paneli: tam ekran geri kaydırması ScrollView / yatay filtrelerle çakışır.
 * Geri için header oku veya Android donanım geri kullanılır.
 */
export const adminStackGestureOptions = {
  gestureEnabled: false,
  fullScreenGestureEnabled: false,
} as const;
